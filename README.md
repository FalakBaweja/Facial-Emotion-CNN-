# Facial-Emotion-CNN-
Custom Convolutional Neural Network (CNN) built from scratch in PyTorch to classify facial expressions from the FER-2013 dataset, featuring data augmentation and an oversampling pipeline for class imbalance.
